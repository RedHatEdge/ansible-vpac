---
# Host baseline: subscription, repos, packages, hostname, /etc/hosts, firewall,
# journald sizing. No networking changes yet.

- name: Host baseline
  hosts: vpac_cluster
  gather_facts: true
  roles:
    - role: host_baseline
