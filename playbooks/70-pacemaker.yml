---
# Pacemaker base: install pcs, configure corosync on the DEDICATED heartbeat
# network (not mgmt), auth hacluster, start cluster.

- name: Pacemaker base
  hosts: pacemaker_cluster
  gather_facts: true
  roles:
    - role: pacemaker_base
