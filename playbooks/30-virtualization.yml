---
# Virtualization: libvirt, KVM, tuned, hugepages, kernel cmdline with isolcpus.
# Hugepage count is computed from vm_catalog unless explicitly overridden.

- name: Virtualization
  hosts: vpac_cluster
  gather_facts: true
  roles:
    - role: virtualization
