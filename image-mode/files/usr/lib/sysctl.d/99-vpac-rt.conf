# Let SCHED_FIFO RT threads (the guest vCPUs) use 100% of their isolated cores.
# Default kernel behaviour throttles RT tasks to 95% of each second as a
# runaway safeguard; a pinned, isolated RT vCPU needs the whole core.
kernel.sched_rt_runtime_us = -1
