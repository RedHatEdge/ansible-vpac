---
galaxy_info:
  author: Stephen Smith
  description: RT kernel tuning — sched_rt_runtime_us, cpufreq governor, RT chrony overrides on every rt_hosts member
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions:
        - "9"

dependencies: []
