---
galaxy_info:
  role_name: builder_mirror
  author: Stephen Smith
  description: >-
    Reposync RHSM repos to a local path and serve them over HTTP so
    air-gapped cluster nodes can install RPMs from the builder host.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions: ["9"]
  galaxy_tags:
    - vpac
    - builder
    - mirror
    - reposync
dependencies: []
