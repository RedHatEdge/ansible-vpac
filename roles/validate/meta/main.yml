---
galaxy_info:
  author: Stephen Smith
  description: End-to-end cluster validation — RT geometry, cyclictest, ceph health, pacemaker + corosync, PTP / chrony, VM RT XML invariants. Read-only.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions:
        - "9"

dependencies: []
