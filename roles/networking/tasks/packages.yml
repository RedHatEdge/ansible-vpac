---
- name: "Install nmstate tooling"
  ansible.builtin.dnf:
    name:
      - nmstate
      - NetworkManager
    state: present

- name: "Ensure NetworkManager is enabled and running"
  ansible.builtin.systemd_service:
    name: NetworkManager
    state: started
    enabled: true
