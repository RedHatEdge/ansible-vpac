---
galaxy_info:
  author: Stephen Smith
  description: Define VMs in libvirt, wrap as Pacemaker VirtualDomain resources, apply placement constraints from vm_catalog
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions:
        - "9"

dependencies: []
