---
galaxy_info:
  role_name: common_tools
  author: Stephen Smith
  description: >-
    Install operator-expected RHEL tooling (vim, tmux, htop, sos, etc.)
    and the Cockpit web console on every vPAC host. Included from
    host_baseline (cluster nodes) and 01-build-builder.yml (builder)
    so both kinds of host get the same operator experience.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions: ["9"]
  galaxy_tags:
    - vpac
    - cockpit
    - tools
    - baseline
dependencies: []
